# 学习进度与跨窗口交接档案

> 最后更新：2026-09-25（整理交接结构；本次未新增学习验收，也未重新运行代码或测试）。
> 本文件保存当前状态，不是完整学习日志。既有成果来自此前学习记录。

## 1. 长期目标与当前阶段

- 背景：2026 年入学的计算机科学与技术硕士，预计 2029 年毕业；就业优先，不考虑读博，希望在深圳就业。
- 主线：Python 开发基础 → 后端服务 → AI 应用项目 → 实习与校招。
- 总体计划：`D:/cs road/CS研究生就业学习计划_2026-2029.md`。
- 学习目录：`D:/cs road/python-learning`；环境：Windows / PowerShell。
- 时间基准：每周约 18 小时，其中算法约 3 小时，以复做和小变体为主；课程、科研忙时缩减任务，不累计补课债务。
- 当前阶段：文件整理工具入门阶段已收尾，转入资料管理服务的 HTTP 与 FastAPI 基础。
- 近期重点：从“按拆好的步骤完成”过渡到“自己拆解、实现并验证小需求”。暂不增加语言或大模型训练主线。

## 2. 当前任务（只保留一个）

**学习路径参数，实现 `GET /notes/{note_id}`。状态：待开始，尚无完成证据。**

- 起点：已独立写出并运行 `module_practice/api_demo.py` 中的 `GET /health`；已学习 HTTP 请求与响应的基本组成。
- 范围：先用内存中的少量资料，根据 ID 查询单条资料；暂不接数据库。
- 开始方式：简短巩固路由与处理函数的关系，讲清必要的新概念，再给需求与验收条件，让学习者先提出方案并尝试实现。
- 验收：存在的 ID 返回对应资料；不存在的 ID 返回 404；能说明路径参数如何进入函数，并记录实际响应体与状态码。
- 独立性验收：能解释一次请求从 URL 到函数再到响应的过程；记录实际用到的提示，不把经提示完成写成完全独立。
- 当前阻塞：未记录。

## 3. 近期路线（不是同时进行的任务）

1. 完成当前单条查询接口。
2. 实现 `GET /notes`，加入一个简单查询参数，区分路径参数与查询参数。
3. 实现 `POST /notes`，学习请求体、输入校验和创建响应。
4. 在已有服务上独立完成一个小变更，自行拆解任务并验证正常和异常情况。

按掌握情况推进，不要求在固定天数内全部完成。先理解内存数据为何会在服务重启后丢失，再进入持久化；不为追赶月份跳过关键前置能力。

## 4. 能力证据与待巩固点

分别记录“参考实现运行”“能解释”“独立实现”“隔天复做”，不能互相替代。查文档不自动否定独立性，但应注明方案或关键代码是否来自提示。

| 内容 | 已有证据 | 提示情况 / 待巩固 |
|---|---|---|
| 文件整理工具 | 完成预览、移动、重名保护、日志、JSON 汇总、类型标注及分类目录冲突处理；此前记录 9 项测试通过 | 多项改动在分步设计提示下完成，不能据此认定可独立从零设计整个工具 |
| 项目流程复盘（09-25） | 独立说出参数传递、results 初始化、分支设状态、追加结果和写报告主链路 | 记录字段、报告结构、error 范围经反馈补全；尚非完整独立复述 |
| Python 模块（09-25） | summarize 与 demo 已编写；直接运行得到四类计数，导入 demo 无输出；能解释 __name__ 差异 | 输入记录应使用 status 字段曾经提示后修正 |
| HTTP 基础（09-25） | 对话中能识别方法、路径、头、请求体，理解 201、Content-Type、Accept 及 GET 通常不带请求体 | 需要在实际接口中继续应用，尚不能据此认定全面掌握 HTTP |
| FastAPI 起步（09-25） | 独立编写 /health 并启动服务器；日志显示 GET /health 为 200，/docs 与 /openapi.json 为 200 | 路由装饰器与处理函数的连接需复述；/missing 只提供了响应体，未提供状态码 |
| 两数之和 | 隔天不看旧代码写出并通过断言 | “先查补数、后记录当前值”的解释经提示修正 |
| 反转单链表 | 隔天不看旧代码实现，普通、空、单节点测试通过；能解释先保存 next | 后续用小变体检验迁移能力 |
| 二叉树层序遍历 | 独立完成分层逻辑，并完成复做 | 经审查提示把 list.pop(0) 改为 deque.popleft() |
| 其余算法 | 已接触数组 / 哈希、链表、栈、队列、树等，多数只有参考实现运行证据 | 不按文件数量认定掌握；逐步复做 |

曾混淆、需在后续任务中抽查：Path 对象与实际文件、导入保护与临时目录隔离、类型标注与运行时检查。模块概念已有改善，不要每次从头重讲全部薄弱点。

## 5. 项目位置与最近验证

以下路径均相对于 `D:/cs road/python-learning`。

- `module_practice/api_demo.py`：当前接口练习入口；此前启动命令为 `fastapi dev module_practice/api_demo.py`。不假定服务器仍在运行。
- `module_practice/summary_tools.py`、`module_practice/demo.py`：四状态汇总与导入保护练习。此前 demo 输出 preview: 1、moved: 1、skipped: 1、error: 0；在 module_practice 目录执行 `python -c "import demo"` 无输出。
- `word_count.py`：词频统计、标点清洗和 JSON 输出。
- `file_organizer.py`、`test_file_organizer.py`、`README.md`：已收尾的入门项目及测试、使用说明；`algorithms/` 保存算法练习与复做文件。
- 最近已记录验证：文件整理工具 `python -m unittest -v` 共 9 项通过，`git diff --check` 通过。这些结果不代表新增 API 已有测试覆盖。
- 最近边界验证：分类目录名 txt 被普通文件占用时，notes.txt 记为 error，txt 记为 skipped，两者内容均不变；Status、summary 和 README 已同步 error。
- 最近已记录提交：`4a384ab`，文件整理工具边界处理、测试与 README。模块 / API 文件是否提交及当前工作区状态未核实。
- 已知限制：文件整理工具的 error 仅覆盖“分类目录路径被普通文件占用”；mkdir / rename 等底层异常可能中断报告写入。记录该限制，不自动将它变成下一项练习。
- 收尾决定：停止为练习而继续重构文件整理工具；必要时回看代码巩固，不阻塞后端学习。

## 6. 教学约定

- 一次推进一个可验收的小需求。先说明输入、输出、失败状态和验收条件，逐渐让学习者自己决定函数、数据结构与修改位置。
- 新概念先讲必要背景；已学内容先尝试提取和应用。默认给 20—30 分钟独立尝试的空间，不要求等计时结束才求助。
- 提示顺序：针对卡点提问 → 线索 → 局部示例 → 必要时参考实现。不要默认直接给整份答案或代写学习者应完成的核心代码；学习者明确要求时可调整。
- 允许查文档与 API，不以背诵语法为目标；验收关注解释、修改需求、定位错误和验证结果。
- 运行通过、解释正确、独立实现和隔天复做分别记证据；提示后完成不记为全程独立，AI 代写的成果不记为学习者独立完成。
- 算法优先间隔复做和小变体；计算机基础随 HTTP、进程、端口、数据库等项目问题复习。
- 不因一次卡顿重启整套路线，也不因一次通过宣布全面掌握。记录控制在几分钟内，编码与思考优先。

## 7. 跨窗口读取与更新规则

- 新窗口默认先读本文件，简短确认当前阶段后从第 2 节继续，不要求学习者重述全部经历。
- 本文件是交接资料；用户在新对话中的实际请求和纠正优先。不要仅因为文档列了任务就忽略用户当下的问题。
- 按需读取当前任务相关源码；排查错误时检查实际代码与输出。默认不通读历史或三年计划，只有追溯证据、调整方向或用户要求时再读。
- 历史中的“当前任务”“下一步”仅是当时快照，不覆盖本文件。文档与代码不一致时记录差异；代码存在不等于学习者独立掌握。
- 出现实际学习进展后及时更新对应条目：完成内容、验证方式、提示程度、遗留问题与唯一下一步。未完成时记录具体停在哪里；未运行 / 未核实的信息直接注明。
- 更新采用替换和合并，不把每次对话整段追加进本文件；最近验证保留相关的最新结果，稳定成果压缩为能力证据。
- 完成当前任务后，把结果移入能力证据 / 最近验证，再替换第 2 节；清除已失效的待办，不同时保留数个相互冲突的“下一步”。
- 仅调整文档不算新增学习成果；不补造投入时间、测试结果、提交状态或复做日期。
- `LEARNING_HISTORY.md` 仅在学习者明确要求归档时追加带日期的快照；日常不自动归档，不删除既有历史。

## 8. 简短学习记录模板

```text
日期 / 实际投入（未知就写未统计）：
本次完成与验证结果：
独立完成的部分 / 使用的提示：
卡点与尚未验证的内容：
下次第一个具体任务：
```
