# Python 学习进度档案

> 目标路线：Python 开发基础 → 后端服务 → AI 应用项目 → 实习与校招  
> 学习计划来源：`D:\cs road\CS研究生就业学习计划_2026-2029.md`  
> 使用方式：每次学习结束后更新“本次记录”和“下一次学习”；更换聊天窗口时，把本文件内容发给我，或直接告诉我先阅读它。

## 当前阶段

- 阶段：2026 年 9 月，恢复 Python 开发能力。
- 当前项目：文件整理工具（首个可交付版本已完成，进入独立小改动与边界验证）；算法转入复做巩固。
- 本月核心目标：熟悉列表、字典、函数、文件、JSON、异常、Git 和终端操作；完成有预览、日志、重名处理的文件整理工具。
- 暂不学习：C++、深度学习、Agent。

## 能力证据与教学方式

以后不再以“复制参考代码后运行通过”作为掌握判断。每项能力按下列证据记录：

- **参考实现已运行**：能在本地运行，知道大致用途；仍需要脱离答案复做。
- **能解释关键逻辑**：能说明数据结构、边界条件与复杂度。
- **独立完成**：先独立尝试，再经代码与测试验证。
- **隔天复做通过**：不看参考资料仍能完成，是本阶段最可靠的掌握证据。

已获得的较强证据：

- 两数之和：独立补全“先查补数、后记录当前值”核心逻辑，并正确说明平均时间 O(n)、空间 O(n)。
- 存在重复元素：独立补全集合成员判断与写入核心逻辑。
- 有效字母异位词：独立补全计数表核心逻辑。
- 文件整理工具：已在演示目录完成预览与实际移动，JSON 报告版本的 6 项自动化测试已通过；仍需通过新的小需求验证独立修改能力。

本次独立修改证据：根据“结果统计应从 `results` 派生”的设计提示，完成 JSON `summary` 汇总；预览计数与真实重名跳过计数均由临时目录测试验证通过。两处非功能性清理已完成，完整测试套件通过并已提交。后续独立新增实际移动的端到端报告测试；首次失败后能根据输出定位“`Path` 对象不等于已创建文件”，补充文件写入后通过。

项目复盘证据：能解释预览不移动且记录 `preview`、重名时源文件保留且记录 `skipped`，并指出必须排除 `organizer_report.json`，防止工具处理自身输出。

命令行边界证据：不存在目录与“文件而非目录”均输出相应错误并以退出码 1 结束；空目录以退出码 0 安全结束，并生成 `mode: preview`、三项计数皆为 0 的报告。

模块基础证据：能区分导入保护与临时目录两层职责——前者防止导入时自动调用 `main()`，后者限制测试读写范围。

隔天复做证据：2026-09-20 未查看旧实现，重新写出两数之和的哈希表解法并通过 3 个断言；能修正说明为“字典记录已遍历的数值与下标，当前轮先查补数”。

隔天复做证据：2026-09-20 未查看旧实现，重新写出单链表反转的三指针实现，构造与转换辅助函数均可运行，并通过普通链表、空链表、单节点链表 4 个断言。

隔天复做证据：2026-09-20 未查看旧实现，重新写出二叉树层序遍历；经代码审查后将列表 `pop(0)` 修正为 `deque.popleft()`，并通过普通树、空树、单节点 3 个断言。

其余算法文件目前按“参考实现已运行”记录，待闭卷复做后再升级状态。

## 当前巩固安排

1. 闭卷复做：两数之和、反转单链表、二叉树层序遍历。每题先独立尝试 20—30 分钟；卡住后只给逐级提示，不立即给完整答案。
2. 项目验收：为文件整理工具增加本次运行的 `preview`／`moved`／`skipped` 数量汇总。先说明你准备改哪一个函数、怎样测试，再动手实现。
3. 算法比例回归：项目与 Python 基础为主，算法每周约 3 小时；当前暂缓继续扩展到新的数据结构题。

## 已有交付（含参考实现已运行）

- [x] 创建项目目录 `D:\cs road\python-learning`。
- [x] 创建并激活 Python 虚拟环境 `.venv`。
- [x] 初始化 Git 仓库。
- [x] 编写并运行 `word_count.py`：读取文本、转小写、按空白切词、使用 `Counter` 统计并输出前十个词。
- [x] 验证文件不存在时使用 `try/except FileNotFoundError` 给出提示。
- [x] 添加 `.gitignore`，忽略 `.venv/`、Python 缓存、编辑器配置和 `.env`。
- [x] 将此前误提交的 `.venv` 从 Git 追踪中移除，且本地虚拟环境保留。
- [x] 为词频统计加入常见英文标点清洗，并将统计结果保存为 `word_count.json`。
- [x] 实现 `file_organizer.py` 的扫描与预览：使用 `pathlib`、`argparse` 和 `logging`，按扩展名显示计划目标路径，但不移动文件。
- [x] 实现安全执行模式：`--apply` 才创建分类目录并移动文件；默认模式仍只预览；日志记录预览与实际移动。
- [x] 为 `get_category()` 编写 3 个 `unittest` 测试（常规扩展名、多点文件名、无扩展名），并通过 `python -m unittest -v` 验证。
- [x] 编写项目 `README.md`，包含功能、预览与执行命令、安全规则和测试命令。
- [x] 使用 `TemporaryDirectory()` 完成“预览不移动”和“重名目标跳过”两项安全测试；`python -m unittest -v` 共 5 项通过。
- [x] 完成哈希表解法的两数之和：正确遵循“先查补数、后存当前数”，并能解释平均时间复杂度 O(n)、空间复杂度 O(n)。
- [x] 完成集合解法的“存在重复元素”：先判断元素是否已出现，再写入集合；能区分集合的成员判断与字典的值到下标映射。
- [x] 为 JSON 报告新增自动化测试；完整文件整理工具测试套件共 6 项通过。
- [x] 完成字典计数解法的有效字母异位词：能用 `dict.get(..., 0)` 建立计数表，并说明集合无法区分重复次数。
- [x] 完成滑动窗口解法的最长无重复字符子串：用集合维护窗口，`left` 与 `right` 都只向右移动，时间复杂度为 O(n)。
- [x] 完成字典分组解法的字母异位词分组：用排序后的单词作为键，字典值保存同组单词列表。
- [x] 完成集合解法的最长连续序列：只从连续段起点扩展，避免重复扫描连续数字。
- [x] 完成单链表反转：使用 `previous`、`current`、`next_node` 三个引用，原地反转节点链接。
- [x] 完成两个有序链表合并：使用 dummy 节点和 tail 指针复用已有节点，并正确处理一侧先耗尽的情况。
- [x] 完成快慢指针检测链表环：用节点对象身份判断相遇，以 O(1) 额外空间区分有环与无环链表。
- [x] 完成删除链表倒数第 N 个节点：让 fast 先走 `n + 1` 步，并通过 dummy 节点统一处理删除头节点。
- [x] 完成栈解法的有效括号：用后进先出顺序匹配最近未闭合的左括号。
- [x] 完成最小栈：使用平行的最小值栈，使 `push`、`pop`、`top`、`get_min` 均为 O(1)。
- [x] 完成两个栈实现队列：用延迟转移把 LIFO 栈组合为 FIFO 队列，并理解其摊还 O(1) 成本。
- [x] 完成二叉树前序遍历：按根－左－右顺序递归访问节点，并正确处理空树。
- [x] 完成二叉树层序遍历：用 `deque` 按层处理节点，并用固定的队列长度划分层级。
- [x] 完成二叉树最大深度：以“空树为 0、非空树为 1 + 子树最大深度”递归计算树高。
- [x] 完成 BST 验证：递归传递祖先定义的开区间，避免只比较直接父子节点的错误。

## 当前代码与已掌握概念

| 内容 | 当前理解与证据 |
|---|---|
| `Path.read_text()` | `word_count.py` 用 UTF-8 读取 `sample.txt`。 |
| 列表与字符串 | `text.lower().split()` 生成单词列表。 |
| `Counter` | 统计每个单词出现次数，`most_common(10)` 获取前十名。 |
| 函数 | `count_words()` 负责统计；`main()` 负责运行流程。 |
| 异常处理 | 文件不存在时程序不崩溃。 |
| Git 忽略规则 | 已理解：`.gitignore` 不会自动忽略已追踪文件，需用 `git rm -r --cached` 停止追踪。 |
| 自动化测试 | 能用 `unittest` 和 `TemporaryDirectory()` 验证分类、预览不移动和重名不覆盖。 |

## 已完成练习：改进词频统计

### 目标

解决当前输出中 `useful.` 与 `useful` 被视为不同单词的问题，并把统计结果保存为 JSON 文件。

### 要求

1. 用 Python 标准库处理常见英文标点：`, . ! ? ; :`。
2. 忽略空字符串。
3. 保持函数职责清晰：读取/清洗、统计、输出或保存不要全部塞进一个函数。
4. 新增 `word_count.json`，保存词频统计结果。
5. 使用 `try/except` 处理输入文件不存在的情况。
6. 运行验证后写一次有意义的 Git 提交。

### 验收样例

输入：

```text
Python is useful. Python is readable, useful!
```

统计结果应将 `useful.` 与 `useful!` 合并为 `useful: 2`。

## 已完成练习：文件整理工具（扫描与预览）

### 目标

编写 `file_organizer.py`：扫描一个**演示目录**中的文件，按扩展名生成“计划移动到哪里”的预览。第一版绝不移动任何文件。

### 要求

1. 用 `pathlib.Path` 接收目录路径并遍历其中的直接文件；第一版不递归进入子目录。
2. 将文件按扩展名分组；无扩展名的文件归入 `no_extension`。
3. 在终端显示每个文件的原路径和计划目标路径，例如 `notes.txt -> txt/notes.txt`。
4. 使用 `argparse`，使用户能执行 `python file_organizer.py demo_files` 指定演示目录。
5. 使用 `logging` 同时把扫描过程记录到 `organizer.log`。
6. 处理目录不存在、传入的不是目录、空目录三种情况。
7. 不要对自己的真实文件夹执行，必须先创建 `demo_files` 测试目录。

## 已完成练习：文件整理工具（安全执行）

### 目标

为工具增加 `--apply` 开关：默认始终预览；只有明确传入该开关才实际移动文件。遇到重名目标文件必须跳过，绝不覆盖。

### 要求

1. `python file_organizer.py demo_files` 仍只预览。
2. `python file_organizer.py demo_files --apply` 才创建分类目录并移动文件。
3. 目标文件已存在时输出并记录“跳过”，不覆盖、不重命名。
4. 每次成功移动和每次跳过都写入 `organizer.log`。
5. 在 `demo_files` 中测试，不对真实目录执行。

## 已完成练习：自动化测试与项目说明

### 目标

用标准库 `unittest` 为核心分类逻辑写自动化测试，并写 README，让其他人能安全运行项目。

### 要求

1. 新建 `test_file_organizer.py`，测试有扩展名、多个点的文件名和无扩展名三种情况。
2. 执行 `python -m unittest -v`，所有测试通过。
3. 新建 `README.md`，写明功能、虚拟环境、预览命令、`--apply` 命令和安全规则。
4. 不测试真实文件夹；测试文件中只调用 `get_category()`，不调用会移动文件的函数。
5. 将代码、测试、README、演示文件和学习档案提交；`organizer.log` 保持在 `.gitignore` 中，不提交。

## 已完成练习：重名保护的自动化测试

### 目标

使用 `tempfile.TemporaryDirectory()` 创建临时测试目录，验证 `organize_files()` 的两项安全承诺：预览不会移动文件；目标同名文件已存在时，`--apply` 不覆盖也不移动源文件。

### 要求

1. 测试中不使用 `demo_files`，临时目录会在测试结束后自动清除。
2. 增加“预览不移动”测试：源文件仍存在，目标分类目录没有被创建。
3. 增加“重名跳过”测试：已存在的目标内容不变，待整理的源文件仍存在。
4. 运行 `python -m unittest -v`，共 5 个测试通过。

## 已完成练习：数组与哈希表——两数之和

### 目标

掌握哈希表（Python 字典）的“以空间换时间”思路，独立解决两数之和问题。

### 问题

给定整数列表 `nums` 和目标值 `target`，返回两个不同元素的下标，使其相加等于 `target`。假设答案唯一；无解时返回 `None`。

```text
nums = [2, 7, 11, 15], target = 9
答案 = [0, 1]
```

### 要求

1. 新建 `algorithms/two_sum.py`，实现 `two_sum(nums, target)`。
2. 使用一个字典记录已遍历的“数值 → 下标”。
3. 每次循环先检查补数 `target - current_number` 是否已出现，再将当前数写入字典，避免把同一个元素使用两次。
4. 用至少 3 个 `assert` 测试：普通情况、重复数字和无解。
5. 能解释时间复杂度为何从双重循环的 O(n²) 降为 O(n)，以及空间复杂度为何为 O(n)。

## 已完成练习：集合——存在重复元素

### 目标

用集合快速判断一个整数列表中是否有重复元素，并理解集合与字典的适用边界。

### 问题

实现 `contains_duplicate(nums)`：只要列表中任意元素出现至少两次就返回 `True`，否则返回 `False`。

### 要求

1. 新建 `algorithms/contains_duplicate.py`。
2. 遍历列表，使用集合保存已经见过的元素；发现元素已在集合中时立即返回 `True`。
3. 写至少 3 个 `assert` 测试：存在重复、不存在重复、空列表。
4. 解释为何这题适合 `set` 而不是 `dict`。

## 已完成练习：文件与 JSON——生成整理报告

### 目标

复习文件写入、JSON 序列化和异常处理：在文件整理工具中生成一份可机器读取的“预览或执行结果”报告。

### 要求

1. 在 `file_organizer.py` 中新增 `organizer_report.json` 输出；报告至少包含模式（`preview` 或 `apply`）和每个文件的结果。
2. 每条记录至少含：源路径、计划目标路径、状态（`preview`、`moved` 或 `skipped`）。
3. 报告使用 `json.dump(..., ensure_ascii=False, indent=2)` 写入，运行后可用文本编辑器查看。
4. 保持原有安全保证：默认预览不移动，重名不覆盖。
5. 为报告中至少一种状态新增测试；不在测试中触碰 `demo_files`。

## 已完成练习：哈希表——有效的字母异位词

### 目标

继续使用字典做“计数表”：判断两个字符串的字符及其出现次数是否完全一致。

### 问题

实现 `is_anagram(first, second)`。忽略字符顺序，但区分大小写；若两个字符串互为字母异位词则返回 `True`。

```text
"anagram" 与 "nagaram" → True
"rat" 与 "car" → False
```

### 要求

1. 新建 `algorithms/valid_anagram.py`。
2. 若字符串长度不同，立即返回 `False`。
3. 用一个字典统计第一个字符串的每个字符次数；遍历第二个字符串时递减对应次数。
4. 当第二个字符串含有未出现过的字符，或次数不足时返回 `False`。
5. 写至少 4 个 `assert`：正确异位词、非异位词、空字符串、重复字符次数不同。
6. 能解释为什么不只比较集合：集合会忽略字符出现的次数。

## 已完成练习：滑动窗口——最长无重复字符子串

### 目标

学习用双指针维护一个连续窗口，在线性时间内求出字符串中不含重复字符的最长连续子串长度。

### 问题

实现 `length_of_longest_substring(text)`：

```text
"abcabcbb" → 3   # "abc"
"bbbbb" → 1      # "b"
"pwwkew" → 3     # "wke"
```

### 要求

1. 新建 `algorithms/longest_substring.py`。
2. 使用 `left` 和 `right` 表示当前窗口的左右边界，用集合存放窗口内的字符。
3. 每次右边界加入一个字符；若出现重复，就不断右移 `left` 并移出左侧字符，直到窗口恢复无重复。
4. 每轮更新最长窗口长度。
5. 写至少 4 个 `assert`，包括空字符串与全重复字符串。
6. 能解释为什么 `left` 和 `right` 都只向右走，因此时间复杂度是 O(n)。

## 已完成练习：哈希表分组——字母异位词分组

### 目标

学习让字典的值保存一个列表：用规范化后的字符串作为键，把互为字母异位词的单词放入同一组。

### 问题

实现 `group_anagrams(words)`：

```text
["eat", "tea", "tan", "ate", "nat", "bat"]
→ [["eat", "tea", "ate"], ["tan", "nat"], ["bat"]]
```

组之间的顺序不重要；每组内保持输入出现顺序。

### 要求

1. 新建 `algorithms/group_anagrams.py`。
2. 对每个单词将字符排序，得到字典键，例如 `"tea"` 的键为 `"aet"`。
3. 字典值是一个列表；键不存在时创建空列表，再把当前单词加入该列表。
4. 返回所有分组构成的列表。
5. 用一个断言验证示例结果，比较前先将每个组和总结果排序，避免组顺序影响测试。
6. 能说明此解法为什么需要 O(n × k log k) 时间：n 是单词数，k 是单词平均长度。

## 已完成练习：集合——最长连续序列

### 目标

使用集合在平均 O(n) 时间内找出未排序整数数组中最长的连续整数序列长度。

### 问题

```text
[100, 4, 200, 1, 3, 2] → 4  # 序列是 1, 2, 3, 4
```

### 要求

1. 新建 `algorithms/longest_consecutive.py`。
2. 先将所有数字放进集合，消除重复并实现平均 O(1) 成员查询。
3. 只有当 `number - 1` 不在集合中时，才从 `number` 开始向右扩展序列。
4. 写至少 4 个断言：示例、空列表、重复数字、无连续数字。
5. 能解释为何不从每个数字都扩展：那会重复扫描同一连续段。

## 数组／哈希表第一组小结

已完成 6 题：两数之和、存在重复元素、有效字母异位词、最长无重复字符子串、字母异位词分组、最长连续序列。

- 字典：保存下标、计数或分组列表。
- 集合：只关心成员是否出现过。
- 双指针／滑动窗口：维护连续子串或子数组。
- 复杂度意识：用额外 O(n) 空间，将常见双层遍历从 O(n²) 降到平均 O(n)。

## 已完成练习：链表——反转单链表

### 目标

理解节点、`next` 指针及原地修改链接；用三个引用变量反转一条单链表。

### 问题

```text
1 → 2 → 3 → None
变为
3 → 2 → 1 → None
```

### 要求

1. 新建 `algorithms/reverse_linked_list.py`，定义 `ListNode`。
2. 实现 `reverse_list(head)`，用 `previous`、`current` 和 `next_node` 三个变量。
3. 每轮循环先保存原来的下一节点，再反转当前节点的 `next`，最后整体向前推进。
4. 用辅助函数将 `[1, 2, 3]` 转为链表和再转回列表，断言结果为 `[3, 2, 1]`。
5. 额外测试空链表和单节点链表。
6. 能解释为什么必须先保存 `next_node`：一旦改写 `current.next`，原链表后半段的入口会丢失。

## 已完成练习：链表——合并两个有序链表

### 目标

学习使用“哨兵节点”（dummy node）和 `tail` 指针，将两个升序单链表原地连接成一个升序链表。

### 问题

```text
1 → 2 → 4
1 → 3 → 4
合并为：1 → 1 → 2 → 3 → 4 → 4
```

### 要求

1. 新建 `algorithms/merge_two_sorted_lists.py`，练习文件可独立定义 `ListNode` 与辅助转换函数。
2. 创建 `dummy` 节点和 `tail` 指针；每轮把两个当前节点中较小的一个接到 `tail.next`。
3. 被选中的链表指针和 `tail` 都向后移动一格。
4. 循环结束后，将尚未耗尽的链表整体接到 `tail.next`。
5. 写 3 个断言：正常合并、一个空链表、两边都有重复值。
6. 能解释 dummy 节点的作用：避免为第一个结果节点写特殊分支。

## 已完成练习：链表——快慢指针检测环

### 目标

掌握 Floyd 快慢指针算法：用 O(1) 额外空间判断单链表是否有环。

### 原理

慢指针每次走 1 步，快指针每次走 2 步。无环时快指针必先到达 `None`；有环时两个指针会在环内相遇。

### 要求

1. 新建 `algorithms/linked_list_cycle.py`，定义 `ListNode`。
2. 实现 `has_cycle(head)`，初始时快慢指针都指向头节点。
3. 循环条件必须确保快指针及其下一节点都不是 `None`。
4. 每轮先移动慢指针一步、快指针两步；相遇即返回 `True`。
5. 快指针到达末尾则返回 `False`。
6. 构造一个无环链表和一个手动将尾节点指回中间节点的有环链表，写 2 个断言。

## 已完成练习：链表——删除倒数第 N 个节点

### 目标

用 dummy 节点和同向双指针，在一次遍历中删除单链表的倒数第 N 个节点。

### 问题

```text
1 → 2 → 3 → 4 → 5，n = 2
结果：1 → 2 → 3 → 5
```

### 要求

1. 新建 `algorithms/remove_nth_from_end.py`，练习文件可独立定义节点与辅助函数。
2. 让 `fast` 从 dummy 节点开始先走 `n + 1` 步；此时 `slow` 仍在 dummy 节点。
3. 同时移动 `fast` 和 `slow`，直到 `fast` 到达 `None`；此时 `slow.next` 就是待删除节点。
4. 用 `slow.next = slow.next.next` 完成删除，并返回 `dummy.next`。
5. 断言至少覆盖：删除中间节点、删除头节点、只有一个节点。
6. 能解释 dummy 节点如何统一“删除头节点”和“删除其他节点”。

## 已完成练习：栈——有效括号

### 目标

理解栈的后进先出（LIFO）特性，使用列表模拟栈来验证括号是否正确配对和嵌套。

### 问题

实现 `is_valid_parentheses(text)`，仅包含 `()[]{}` 的字符串在所有括号都正确闭合、嵌套顺序也正确时返回 `True`。

```text
"()[]{}" → True
"([)]"   → False
"(]"     → False
```

### 要求

1. 新建 `algorithms/valid_parentheses.py`。
2. 遇到左括号时压入栈；遇到右括号时，栈顶必须是对应的左括号。
3. 栈为空却遇到右括号时立即返回 `False`。
4. 扫描结束后只有栈为空才返回 `True`。
5. 写至少 5 个断言，覆盖正确嵌套、交叉嵌套、未闭合、仅右括号、空字符串。
6. 能解释为什么队列不适合：需要最近遇到的左括号先被匹配。

## 已完成练习：栈——最小栈

### 目标

实现支持 `push`、`pop`、`top` 和 `get_min` 的栈，并让全部操作的时间复杂度都为 O(1)。

### 要求

1. 新建 `algorithms/min_stack.py`，定义 `MinStack` 类。
2. 使用一个普通列表保存栈元素；再使用一个“最小值栈”，其每个位置记录对应普通栈深度下的最小值。
3. `push(value)` 同时更新两个列表；`pop()` 同时弹出两个列表。
4. `top()` 返回普通栈顶部；`get_min()` 返回最小值栈顶部。
5. 写测试覆盖：连续压入、弹出后最小值变化、重复最小值。
6. 能解释为什么每次都保存最小值，才能避免 `get_min()` 扫描整个栈。

## 链表与栈小结

- 链表：指针重连、dummy 节点、快慢指针、同向双指针。
- 栈：后进先出，解决嵌套匹配；可用额外状态栈实现 O(1) 最小值查询。

## 已完成练习：队列——两个栈实现队列

### 目标

用 `input_stack` 和 `output_stack` 模拟先进先出（FIFO）队列，并理解元素只会在两个栈之间转移一次。

### 要求

1. 新建 `algorithms/queue_using_stacks.py`，定义 `MyQueue`。
2. `push(value)` 只压入 `input_stack`。
3. `pop()` 或 `peek()` 需要输出时，若 `output_stack` 为空，就把 `input_stack` 所有元素逐个弹出并压入 `output_stack`。
4. 从 `output_stack` 顶部取出或查看元素；实现 `empty()`。
5. 写测试覆盖：入队、查看队首、出队、交替入队与出队、空队列判断。
6. 能解释为何单次转移可能是 O(n)，但一连串操作的平均成本是 O(1)：每个元素最多入栈两次、出栈两次。

## 已完成练习：二叉树——前序遍历

### 目标

理解二叉树节点的 `left`、`right` 引用及递归基线，完成根－左－右（前序）深度优先遍历。

### 问题

```text
    1
   / \
  2   3
 / \
4   5

前序结果：[1, 2, 4, 5, 3]
```

### 要求

1. 新建 `algorithms/binary_tree_preorder.py`，定义 `TreeNode`，包含 `value`、`left`、`right`。
2. 实现 `preorder(root)`；空节点返回空列表。
3. 当前节点有值时，先记录它，再递归遍历左子树、右子树。
4. 手工构造题目中的树，断言前序结果。
5. 增加空树与单节点树断言。
6. 能说明递归终止条件为什么是 `root is None`。

## 已完成练习：二叉树——层序遍历

### 目标

使用队列按层从左到右遍历二叉树，并将每一层的节点值单独组织为一个列表。

### 问题

```text
    1
   / \
  2   3
 / \
4   5

结果：[[1], [2, 3], [4, 5]]
```

### 要求

1. 新建 `algorithms/binary_tree_level_order.py`，定义独立的 `TreeNode`。
2. 空树直接返回 `[]`；否则以根节点初始化 `collections.deque`。
3. 每轮先记录当前队列长度；只处理这固定数量的节点，形成当前层的结果。
4. 取出节点后，存在左、右子节点时依次加入队列。
5. 写 3 个断言：题目示例、空树、只有根节点。
6. 能解释为什么使用 `deque.popleft()` 而不是 `list.pop(0)`：前者是 O(1)，后者需要移动其余元素，通常是 O(n)。

## 已完成练习：二叉树——最大深度

### 目标

用递归 DFS 计算从根节点到最深叶子节点经过的节点数。

### 问题

```text
    1
   / \
  2   3
 /
4

最大深度：3
```

### 要求

1. 新建 `algorithms/binary_tree_max_depth.py`，定义独立的 `TreeNode`。
2. 空节点深度为 0。
3. 非空节点深度为 `1 + max(左子树深度, 右子树深度)`。
4. 写 3 个断言：不平衡树、空树、单节点。
5. 能比较：DFS 和 BFS 都访问每个节点一次，时间 O(n)；递归 DFS 额外占用 O(h) 调用栈，BFS 额外占用 O(w) 队列，其中 h 是树高、w 是最宽层节点数。

## 已完成练习：二叉搜索树——验证 BST

### 目标

判断一棵二叉树是否满足严格二叉搜索树（BST）性质：每个节点左子树所有值都更小，右子树所有值都更大。

### 要求

1. 新建 `algorithms/validate_bst.py`，定义独立的 `TreeNode`。
2. 递归函数接收当前节点允许的开区间 `(lower, upper)`；空节点返回 `True`。
3. 当前值不满足 `lower < value < upper` 时返回 `False`。
4. 递归左子树时更新上界为当前值；递归右子树时更新下界为当前值。
5. 写 3 个断言：合法 BST、根节点局部看似正确但违反祖先限制的树、空树。
6. 能解释为什么只比较“当前节点与直接孩子”不够：右子树深处的值也必须大于根节点。

## 已完成练习：文件整理工具——结果数量汇总

### 目标

让 `organizer_report.json` 在已有 `mode` 与 `results` 之外，增加 `summary` 字段，汇总本次每种状态的数量。

### 目标示例

```json
{
  "mode": "preview",
  "summary": {
    "preview": 3,
    "moved": 0,
    "skipped": 0
  },
  "results": []
}
```

### 验收方式

1. 先用自己的话说明：你准备修改哪个函数、要新增什么变量或函数、以及为什么放在那里。
2. 在临时目录测试预览报告至少包含正确的 `preview` 数量；不触碰 `demo_files`。
3. 再补一个“重名跳过”的测试，确认 `skipped` 数量正确。
4. 运行 `python -m unittest -v`，并将新增的测试与实现一同提交。

### 当前验收结果

- [x] `summary` 能汇总预览结果。
- [x] 重名跳过时，报告中的 `summary["skipped"]` 为 1。
- [x] 完整测试套件 6 项通过。
- [x] 删除尾随空白，并移除冗余的 `report["summary"] = summary` 赋值后再次运行测试。

## 已完成练习：闭卷复做——两数之和

### 规则

1. 不查看 `algorithms/two_sum.py`，也不查看早先聊天中的参考实现。
2. 先花最多 20 分钟独立写思路、函数和至少 3 个断言；需要帮助时只说明卡住的具体位置。
3. 本题通过后，必须用自己的话解释“为何先查补数、再记录当前数”。
4. 只有代码、测试与解释均正确，才记录为“隔天复做通过”。

### 题目

实现 `two_sum(nums, target)`：返回两个**不同元素**的下标，使对应数值之和为 `target`；没有答案时返回 `None`。

### 验收结果

- [x] 不查看旧实现完成函数。
- [x] 3 个断言通过，终端输出“`两数之和闭卷复做通过`”。
- [x] 能解释先查补数、后记录当前数的原因。

## 已完成练习：闭卷复做——反转单链表

### 规则

不查看 `algorithms/reverse_linked_list.py`。先独立定义节点和 `reverse_list(head)`，并写出普通链表、空链表和单节点三种断言。需要帮助时只说明具体卡点。

### 验收结果

- [x] 独立实现 `previous`、`current`、`next_node` 三指针反转。
- [x] 普通链表、空链表和单节点链表断言均通过。
- [x] 能解释必须先保存原始下一节点。
- [ ] 代码质量改进（可与下次提交一起完成）：将允许空值的 `head` 标为 `ListNode | None`；让“链表转列表”对空链表返回 `[]`，并直接断言 `reverse_list(None) is None`。

## 已完成练习：闭卷复做——二叉树层序遍历

### 规则

不查看 `algorithms/binary_tree_level_order.py`。独立定义 `TreeNode` 和 `level_order(root)`；使用 `collections.deque`，并写出普通树、空树、单节点三种断言。完成后解释为什么每层要先固定 `len(queue)`。

### 验收结果

- [x] 独立完成按层分组与固定队列长度逻辑。
- [x] 初版实现被审查发现 `list.pop(0)` 的复杂度问题，并独立改用 `deque.popleft()`。
- [x] 普通树、空树和单节点树断言均通过。
- [x] 能解释固定队列长度用于隔离本层与下一层节点。

## 当前练习：文件整理工具——`moved` 报告测试

### 目标

在不改动应用代码的前提下，新增临时目录测试，证明实际移动文件后 JSON 报告中的 `summary["moved"]` 正确。

### 验收方式

1. 临时目录中只创建一个根目录文件，例如 `report.pdf`。
2. 调用 `organize_files(source_dir, apply=True)`，再调用 `write_report(...)`。
3. 读取 JSON 报告，断言 `moved == 1`、`preview == 0`、`skipped == 0`。
4. 同时断言源文件已不存在，目标 `pdf/report.pdf` 存在。
5. 运行完整测试套件，预期从 6 项增加到 7 项通过。

### 验收结果

- [x] 临时目录中创建 `report.pdf`，实际执行移动后源文件不存在、目标文件存在。
- [x] JSON 报告统计为 `preview: 0`、`moved: 1`、`skipped: 0`。
- [x] 初版测试未创建真实文件而失败；根据输出修正后，完整套件 7 项通过。

## 当前练习：命令行边界验证

### 目标

不改代码，直接用命令行验证工具已经实现的三种输入边界：不存在路径、文件路径而非目录、空目录。

### 验收方式

1. 运行工具并传入不存在的目录；记录报错信息和 PowerShell 的 `$LASTEXITCODE`。
2. 传入项目中的 `sample.txt`；记录报错信息和 `$LASTEXITCODE`。
3. 创建一个全新的空演示目录，在默认预览模式运行；确认程序不移动文件，并生成一个 `summary` 三项均为 0 的报告。
4. 不需要修改代码；将三次命令和关键输出发来复盘。

### 验收结果

- [x] 不存在目录：退出码 1。
- [x] 非目录路径：退出码 1。
- [x] 空目录：退出码 0，生成全 0 的预览报告。

## 当前练习：Python 模块——`__name__` 与导入安全

### 目标

理解同一个 Python 文件被“直接执行”与被“导入”时的区别，解释为何测试导入 `file_organizer` 时不会意外运行命令行主程序。

### 要求

1. 不查看资料，先用自己的话解释 `if __name__ == "__main__":` 的作用。
2. 运行 `python -c "import file_organizer; print(file_organizer.__name__)"`，记录输出。
3. 说明：若执行 `python file_organizer.py --help`，这个文件中的 `__name__` 会是什么，以及为什么。

### 验收结果

- [x] 正确说明直接执行时条件成立、导入时条件不成立。
- [x] 运行导入命令，得到 `file_organizer`。
- [x] 经纠正后，能说明直接运行时 `__name__` 为 `"__main__"`。
- [x] 能区分导入保护与 `TemporaryDirectory()` 的职责。

## 当前练习：函数职责——提取纯函数

### 目标

将 JSON 报告的状态统计从 `write_report()` 中提取成一个独立、无文件读写的函数，使统计逻辑可直接测试。

### 要求

1. 先设计函数名、参数和返回值：输入是 `results`，输出应是三种状态的汇总字典。
2. 函数不应打印、写文件、读文件或依赖目录路径；它只计算并返回数据。
3. `write_report()` 改为调用这个函数来得到 `summary`。
4. 新增直接测试：混合 `preview`、`moved`、`skipped` 至少各一个，断言完整统计字典。
5. 先把设计（函数签名＋测试数据）发来确认，再改代码。

### 验收结果

- [x] 设计并实现 `create_status_summary(results)`，只接收数据、返回数据，不进行 I/O。
- [x] `write_report()` 通过该函数获得 `summary`。
- [x] 为混合三种状态新增直接单元测试，完整套件增至 8 项通过。
- [x] 完成代码整理：删除旧的注释循环、移动测试中的局部导入到文件顶部、清理尾随空白，并让预览报告测试只使用 `preview` 结果。

## 当前练习：类型标注——描述文件处理结果

### 目标

理解普通 `dict[str, str]` 的局限，并用 `TypedDict` 为单条文件处理结果写出明确的字段约定；先只设计，不改运行逻辑。

### 背景

当前每条 `result` 都应包含 `source`、`target`、`status`。普通字典无法让读代码的人立即知道哪些键是必需的。

### 要求

1. 先写出 `FileResult` 中应有的全部字段和它们的类型。
2. 说明 `TypedDict` 与普通运行时类的区别：它主要在什么时候提供帮助？
3. 暂不修改 `file_organizer.py`；先把设计发来确认。

### 设计验收结果

- [x] 正确列出 `source`、`target`、`status` 三个字段，并限制状态为三种允许值。
- [x] 理解 `TypedDict`／`Literal` 主要由编辑器与类型检查工具使用，运行时不会自动拒绝错误字符串。
- [x] 选择“先确定 `status`、后统一构造并追加完整结果”的结构，能减少分支重复并满足必填字段约定。

### 实现与回归要求

1. 在 `file_organizer.py` 顶部定义 `Status` 与 `FileResult`。
2. 将 `organize_files()` 返回类型和 `results` 的类型改为 `FileResult` 对应类型。
3. 循环中每个分支只确定 `status`、执行已有日志或移动操作；分支结束后统一创建完整 `result` 并追加一次。
4. 将 `create_status_summary()` 和 `write_report()` 的 `results` 参数类型同步更新。
5. 不改变原有功能，运行 8 项测试验证。

### 当前验收结果

- [x] 已定义 `Status` 与 `FileResult`。
- [x] 已将三种分支重构为“确定状态后统一创建结果”，8 项运行时测试通过。
- [x] 类型标注已补齐：`organize_files()` 的返回值与 `results` 局部变量、`create_status_summary()` 参数、`write_report()` 参数均使用 `FileResult`；`status` 局部变量标为 `Status`。

## 当前练习：类与对象——`TypedDict` 和 `dataclass`

### 目标

理解 `TypedDict` 虽然使用 `class` 语法，但运行时仍是普通字典；理解何时应使用真正的对象类（例如 `dataclass`）。本节先做概念判断，不修改项目。

### 思考题

若未来希望 `FileResult` 自带一个 `to_display_text()` 方法来格式化显示结果，应该继续用 `TypedDict`，还是改用 `dataclass`？说明原因。

## 待学习队列

- 二分查找：排序数组中的目标值查找（已讲解，尚无独立完成证据）。
- 之后再按巩固情况决定是否继续二分变体、树或项目下一阶段。

## 下一次学习

1. 闭卷复做两数之和；不看旧代码，先写思路和测试。
2. 完成三种命令行边界验证。
3. 区分 `TypedDict` 与 `dataclass`，再决定项目是否需要引入真实对象。

## 本次记录

- 日期：2026-09-18
- 实际投入：约 1 小时（估计，待学习者确认）。
- 交付：可运行的词频统计程序、JSON 统计结果、样例文本、Git 仓库和 `.gitignore`。
- 发现的问题：简单的 `split()` 不会去掉单词两端的标点；已通过清洗规则解决。
- 已解决的问题：`.venv` 曾被提交；已通过 `git rm -r --cached .venv` 停止 Git 追踪，本地环境仍可使用。
- 新增交付：`file_organizer.py` 已成功预览 `csv`、`txt`、`jpg` 和 `no_extension` 四类文件，并在日志中记录完整计划。
- 新增交付：在 `demo_files` 中用 `--apply` 成功移动四个演示文件，目标目录结构已由 PowerShell 递归检查确认。
- 新增交付：`get_category()` 的 3 个测试全部通过；README 已包含使用与安全说明；相关文件已提交，Git 工作区干净。
- 新增交付：重名保护的两项临时目录测试通过，完整测试套件共 5 项通过；Git 工作区干净。
- 新增交付：两数之和的核心哈希表代码与复杂度解释正确；理解了为什么必须先查补数再记录当前元素。
- 新增交付：存在重复元素的集合核心代码正确；能解释集合与字典分别适合什么问题。
- 新增交付：JSON 报告能够记录预览或执行模式及逐文件状态；对应自动化测试通过，完整测试套件共 6 项通过。
- 新增交付：有效字母异位词的字典计数核心代码正确；理解了长度预检与计数不允许为负数的作用。
- 新增交付：最长无重复字符子串的滑动窗口代码及 4 个断言运行通过；有效字母异位词程序也已执行通过。
- 新增交付：字母异位词分组使用“排序字符串为键、列表为值”的哈希表结构实现，断言运行通过。
- 新增交付：最长连续序列的集合代码及 4 个断言运行通过；数组／哈希表第一组共完成 6 题。
- 新增交付：单链表反转的三指针实现及空链表、单节点、普通链表测试均运行通过。
- 新增交付：合并两个有序链表的 dummy + tail 实现及 3 个断言运行通过。
- 新增交付：检测链表环的快慢指针实现及有环、无环断言运行通过。
- 新增交付：删除倒数第 N 个节点的 dummy + 双指针实现及 3 个断言运行通过。
- 新增交付：有效括号的栈实现及 6 个断言运行通过。
- 新增交付：最小栈的平行最小值栈实现及重复最小值测试运行通过。
- 新增交付：两个栈实现队列的交替操作和空队列测试运行通过。
- 新增交付：二叉树前序遍历的普通树、空树和单节点测试运行通过。
- 新增交付：二叉树层序遍历的普通树、空树和单节点测试运行通过。
- 新增交付：二叉树最大深度的不平衡树、空树和单节点测试运行通过。
- 新增交付：BST 验证的合法树、祖先范围违规树和空树测试均运行通过。
- 进度校准：历史“完成”记录主要代表代码已运行；后续以独立完成、关键逻辑解释和隔天复做作为掌握证据。
- 新增交付：JSON `summary` 的预览与真实跳过统计均通过临时目录测试验证；代码清理、6 项测试和 Git 提交均已完成。
- 新增交付：两数之和闭卷复做通过，具备代码、断言和关键顺序解释三类证据。
- 新增交付：反转单链表闭卷复做通过，具备独立代码、4 个断言和关键指针顺序解释三类证据。
- 新增交付：二叉树层序遍历闭卷复做通过，能在审查反馈后将出队操作修正为 O(1) 的 `deque.popleft()`。
- 新增交付：独立添加实际移动的端到端报告测试；修正临时文件未创建的问题后，完整项目套件 7 项通过。
- 新增交付：README 已补充报告格式、状态含义、测试覆盖和“当前共有 7 项自动化测试”的说明，并已提交。
- 新增交付：项目口头复盘通过，能解释预览、跳过和报告文件排除逻辑。
- 新增交付：不存在路径、非目录路径和空目录的命令行边界验证均符合预期。
- 新增交付：理解 `__name__` 导入保护与临时目录测试隔离的不同职责。
- 新增交付：完成 JSON 状态统计纯函数的设计、实现与直接测试，8 项测试通过；待完成小型代码整理后提交。
- 新增交付：纯函数重构代码已整理，8 项测试与格式检查通过，并已提交。
- 新增交付：完成 `FileResult` 的字段与状态限制设计，理解类型检查与运行时的区别。
- 新增交付：`FileResult` 类型标注重构完成，所有接口与局部变量已同步，8 项回归测试通过。
- 下次第一个具体任务：区分 `TypedDict` 与 `dataclass` 的适用边界。

## 每次汇报模板

```text
学习日期：
实际投入：
完成内容：
运行命令与结果：
遇到的问题：
我现在能独立解释或修改的部分：
下次想继续的任务：
```
