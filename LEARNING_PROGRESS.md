# Python 学习进度档案

> 路线：Python 开发基础 → 后端服务 → AI 应用项目 → 实习与校招
> 依据：`D:\\cs road\\CS研究生就业学习计划_2026-2029.md`
> 最后校准：2026-09-22。更换聊天窗口后，让助教先读取本文件；需要完整过程时再查阅 `LEARNING_HISTORY.md`。

## 当前任务（唯一）

文件整理工具的首个版本已完成并准备提交。提交后进行一次简短复盘：说明预览、移动、跳过和错误如何流入 JSON 报告；随后转入资料管理服务所需的 Python 模块、HTTP 与 FastAPI 基础。

## 最近验收

- 新增并验证“分类目录名被普通文件占用”的边界：根目录 `txt` 是普通文件、根目录 `notes.txt` 需归入 `txt/` 时，`notes.txt` 为 `error`，`txt` 为 `skipped`，两者内容均不变。
- `Status` 和报告 `summary` 均新增 `error`；README 已同步状态说明、安全规则和测试数。
- `python -m unittest -v`：9 项测试通过；`git diff --check` 通过。
- 本次改动待提交：`file_organizer.py`、`test_file_organizer.py`、`README.md`、本学习档案。

## 能力证据（按证据而非题目数量）

### 已有较强证据

- 两数之和：隔天不看旧代码完成并通过断言；“先查补数、后记录当前值”的解释经提示后修正正确。
- 反转单链表：隔天不看旧代码完成，普通、空、单节点测试通过；能解释先保存原 `next`。
- 二叉树层序遍历：独立完成按层分组；经审查提示后把 `list.pop(0)` 改为 `deque.popleft()`。
- 文件整理工具：在分步设计提示下完成报告汇总、真实移动测试、纯统计函数、`FileResult` 类型标注和分类目录冲突边界；当前 9 项测试通过。

### 仍需巩固

- 多数算法文件仅有“参考实现已运行”证据；不以此认定掌握。
- 容易混淆的概念：`Path` 对象与实际文件、导入保护与临时目录隔离、类型标注与运行时检查。
- 算法保持每周约 3 小时，以间隔复做和小变体为主，暂不继续追求新题数量。

## 项目收尾标准（2026 年 9 月）

1. 提交本次边界处理、测试、README 和学习档案。
2. 做一次项目说明复盘：预览、移动、跳过／错误如何流入 JSON 报告。
3. 之后停止为练习而重构文件整理工具，转入资料管理服务的 Python 模块、HTTP 与 FastAPI 基础。

## 近期已交付

- `word_count.py`：文件读取、词频统计、标点清洗、JSON 输出。
- `file_organizer.py`：预览、`--apply`、重名保护、日志、JSON 报告、`TypedDict`／`Literal` 类型标注。
- `test_file_organizer.py`：当前 9 项自动化测试。
- `README.md`：使用方式、安全规则、报告结构和测试说明。
- `algorithms/`：已有数组／哈希、链表、栈、队列、树等练习；其中只有上方列出的三项有较强闭卷／独立证据。

## 教学约定

- 新需求先说清输入、输出、失败状态和测试方法；先独立尝试 20—30 分钟。
- 卡住时按“提问 → 线索 → 局部示例 → 参考实现”逐级提示。
- “运行通过”“能解释”“独立完成”“隔天复做通过”分别记录，不混用。

## 每次汇报模板

```text
学习日期：
实际投入：
完成内容：
运行命令与结果：
遇到的问题：
我现在能独立解释或修改的部分：
下次想继续的任务：
```
